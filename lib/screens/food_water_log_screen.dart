import 'package:flutter/material.dart';

class FoodWaterLogScreen extends StatelessWidget {
  const FoodWaterLogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Food & Water Log",
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}
